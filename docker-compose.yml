version: '3'
services:

  #APP
  app:
    build:
      context: .
      dockerfile: ./docker/Dockerfile
      args:
        - PHP_V=${PHP_V}
    container_name: ${APP_SID}-app
    restart: always
    tty: true
    environment:
      SERVICE_NAME: app
    working_dir: /var/www
    volumes:
      - ${APP_SRC}:/var/www/:cached
      - ./docker/utils/cron:/etc/cron
      - ./docker/utils/supervisor/conf.d:/etc/supervisor/conf.d
      - ./docker/php/local.ini:/usr/local/etc/php/conf.d/local.ini
      - ./docker/php/opcache.ini:/usr/local/etc/php/conf.d/opcache.ini

  #NGINX
  nginx:
    image: nginx:alpine
    container_name: ${APP_SID}-nginx
    restart: always
    tty: true
    ports:
      - ${HOST_PORT}:80
    volumes:
      - ${APP_SRC}:/var/www/
      - ./docker/utils/cron:/etc/cron
      - ./docker/utils/supervisor/conf.d:/etc/supervisor/conf.d
      - ./docker/nginx/conf.d/:/etc/nginx/conf.d/

  #MYSQL
  mysql:
    image: mysql:${DB_VERS}
    container_name: ${APP_SID}-mysql
    restart: always
    tty: true
    ports:
      - ${DB_PORT}:3306
    environment:
      MYSQL_DATABASE: ${DB_NAME}
      MYSQL_ROOT_PASSWORD: ${DB_PASS}
    command: ['--character-set-server=utf8mb4', '--collation-server=utf8mb4_unicode_ci','--default-authentication-plugin=mysql_native_password']
    volumes:
      - ./docker/mysql/data:/var/lib/mysql/
      - ./docker/mysql/my.cnf:/etc/mysql/my.cnf

  #MEMCACHED
  memcached:
    image: memcached:alpine
    container_name: ${APP_SID}-memcached

  #REDIS
  redis:
    image: redis:alpine
    container_name: ${APP_SID}-redis

  #PHPMYADMIN
  phpmyadmin:
    image: phpmyadmin/phpmyadmin:latest
    container_name: ${APP_SID}-pma
    environment:
      PMA_HOST: mysql
      MYSQL_ROOT_PASSWORD: ${DB_PASS}
    ports:
      - ${PMA_PORT}:80

  #MAILHOG
  mailhog:
    image: mailhog/mailhog:latest
    container_name: ${APP_SID}-mailhog
    expose:
      - 1025
    ports:
      - ${MH_PORT}:8025